# Copyright (c) 2020 Gitpod GmbH. All rights reserved.
# Licensed under the MIT License. See License-MIT.txt in the project root for license information.

{{ $certManager := (index .Values "cert-manager") }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: gitpod-selfsigned-issuer
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ $certManager.ca.certificate.secretName }}
  namespace: {{ .Release.Namespace }}
spec:
  isCA: true
  commonName: {{ $certManager.ca.issuerName }}
  secretName: {{ $certManager.ca.certificate.secretName }}
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: gitpod-selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ $certManager.ca.issuerName }}
  namespace: {{ .Release.Namespace }}
spec:
  ca:
    secretName: {{ $certManager.ca.certificate.secretName }}
